#!/usr/bin/env sh

setbg &		# Set the background
sxhkd &	        # Bind keys
remaps &
uim-xim &
# uim-toolbar-gtk3-systray &
xset r rate 300 50 &                    # Speed xrate up
nightshift -d &
#xrdb ${XDG_CONFIG_HOME:-$HOME/.config}/Xresources
# wal -i ~/.config/wall.png >/dev/null 2>&1 &
unclutter &
picom -b &
dunst &
